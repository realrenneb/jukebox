version: 2

sources:
  - name: customer
    database: raw
    schema: customer
    tables:
      - name: customer_registered
        columns:
          - name: customerid,
            tests:
              - unique
              - not null
          - name: customerdateofbirth,
            tests:
              - not null
              - dbt_utils.date_format:
                  dateformat: '%Y-%m-%d'
                  
models:
          - name: customerdateofbirth,
            tests:
              - not null
              - dbt_utils.date_format:
                  dateformat: '%Y-%m-%d'
