version: 2

sources:
  - name: payment
    database: raw
    schema: payment
    tables:
      - name: payment_received
        columns:
          - name: payment        
            tests:
              - not_null        

models:
  - name: stg_payment_received
    description: "Staging model for payment data, contains raw JSON"
    columns:
      - name: payment
        description: "Raw JSON payment data"
        tests:
          - not_null