version: 2

sources:
  - name: customer
    database: raw
    schema: customer
    tables:
      - name: customer_registered
        columns:
          - name: customerid
            tests:
              - unique
              - not_null
          - name: customeridentitynumber
            tests:
            - unique
            - not_null
          - name: customerdateofbirth
            tests:
              - not_null
              - dbt_utils.date_format:
                  dateformat: '%Y-%m-%d'

models:
      - name: customer_registered
        columns:
          - name: customerid
            tests:
              - unique
              - not_null
          - name: customerdateofbirth,
            tests:
              - not_null
              - dbt_utils.date_format:
                  dateformat: '%Y-%m-%d'
          - name: customerdateofbirth
            tests:
              - not_null
              - dbt_utils.date_format:
                  dateformat: '%Y-%m-%d'
     
 

